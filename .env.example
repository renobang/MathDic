# This static application requires no runtime secrets.
# Local deployment uses `npx wrangler login`.
# For future CI only, configure CLOUDFLARE_API_TOKEN and
# CLOUDFLARE_ACCOUNT_ID in the CI secret store, never in source code.
